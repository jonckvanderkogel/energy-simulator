spring:
  config:
    import: classpath:application-wiremock.yml
  liquibase:
    change-log: classpath:db/changelog.xml

logging:
  level:
    io.r2dbc.postgresql.QUERY: DEBUG
    io.r2dbc.postgresql.PARAM: DEBUG

files:
  power: power_test.csv
  gas: gas_test.csv
